@extends('dash.layouts.app')
@section('title','LOGS')
@section('content')
<div class="row g-4">
  <div class="col-12"><div class="card"><div class="card-body"><h5 class="mb-1">Gateway Logs</h5><p class="text-muted mb-3">Recent application, AI, broadcast and gateway events. Sensitive message bodies and API secrets are not logged here.</p><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Time</th><th>Level</th><th>Category</th><th>Message</th></tr></thead><tbody>@forelse($gatewayLogs as $row)<tr><td class="text-nowrap">{{ optional($row->created_at)->format('d M H:i:s') }}</td><td>{{ strtoupper($row->level) }}</td><td>{{ $row->category }}</td><td>{{ $row->message }}</td></tr>@empty<tr><td colspan="4" class="text-muted">No gateway logs yet.</td></tr>@endforelse</tbody></table></div></div></div></div>
  <div class="col-12"><div class="card"><div class="card-body"><h5>API Requests</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Time</th><th>Client</th><th>Method</th><th>Path</th><th>Status</th><th>ms</th></tr></thead><tbody>@forelse($apiLogs as $row)<tr><td class="text-nowrap">{{ optional($row->created_at)->format('d M H:i:s') }}</td><td>{{ $clients[$row->api_client_id] ?? 'Deleted client' }}</td><td>{{ $row->method }}</td><td><code>{{ $row->path }}</code></td><td>{{ $row->status_code }}</td><td>{{ $row->duration_ms }}</td></tr>@empty<tr><td colspan="6" class="text-muted">No API requests yet.</td></tr>@endforelse</tbody></table></div></div></div></div>
  <div class="col-12"><div class="card"><div class="card-body"><h5>Webhook Deliveries</h5><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Time</th><th>Client</th><th>Event</th><th>Status</th><th>Attempts</th><th>HTTP</th><th>Error</th></tr></thead><tbody>@forelse($webhooks as $row)<tr><td class="text-nowrap">{{ optional($row->created_at)->format('d M H:i:s') }}</td><td>{{ $clients[$row->api_client_id] ?? 'Deleted client' }}</td><td>{{ $row->event_type }}</td><td>{{ $row->status }}</td><td>{{ $row->attempts }}</td><td>{{ $row->response_code ?: '-' }}</td><td class="text-truncate" style="max-width:320px">{{ $row->last_error ?: '-' }}</td></tr>@empty<tr><td colspan="7" class="text-muted">No webhook deliveries yet.</td></tr>@endforelse</tbody></table></div></div></div></div>
</div>
@endsection
