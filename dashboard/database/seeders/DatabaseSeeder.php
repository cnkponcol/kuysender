<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Intentionally empty. Production credentials must never be seeded with defaults.
        // Create the first admin with: php artisan kuysender:admin
    }
}
